# PostgreSQL Development Configuration Example
# Copy this to .env and fill in your values

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here
DICTIONARY_API_KEY=your-dictionary-api-key-here

# PostgreSQL Configuration (for development)
# Format: postgresql://user:password@host:port/database
POSTGRES_CONNECTION=postgresql://honeyspeak:password@localhost:5432/honeyspeak_dev

# Alternative PostgreSQL connection formats:
# Local connection with default port:
# POSTGRES_CONNECTION=postgresql://username:password@localhost/database

# Remote connection:
# POSTGRES_CONNECTION=postgresql://username:password@remote-host.com:5432/database

# Connection with SSL:
# POSTGRES_CONNECTION=postgresql://username:password@host/database?sslmode=require

# SQLite Configuration (used when POSTGRES_CONNECTION is not set)
DATABASE_PATH=/data/honeyspeak/Dictionary.sqlite

# Storage paths
STORAGE_DIRECTORY=/data/honeyspeak
STORAGE_HOME=/data
ASSET_DIRECTORY=/data/honeyspeak/assets_hires
PACKAGE_DIRECTORY=/data/honeyspeak/assets

# API limits
MAX_DICTIONARY_API_CALLS_PER_RUN=100

# SMB Storage Configuration (for Kubernetes deployment)
STORAGE_SERVER=your-smb-server
STORAGE_SHARE=your-share-name
STORAGE_USERNAME=your-username
STORAGE_PASSWORD=your-password